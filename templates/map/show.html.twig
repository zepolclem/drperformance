{% extends 'base.html.twig' %}

{% block title %}Map{% endblock %}

{% block body %}
    <h1>Map</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ map.id }}</td>
            </tr>
            <tr>
                <th>Power</th>
                <td>{{ map.power }}</td>
            </tr>
            <tr>
                <th>Torque</th>
                <td>{{ map.torque }}</td>
            </tr>
            <tr>
                <th>Resume</th>
                <td>{{ map.resume }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ map.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('map_index') }}">back to list</a>

    <a href="{{ path('map_edit', {'id': map.id}) }}">edit</a>

    {{ include('map/_delete_form.html.twig') }}
{% endblock %}
