{% extends 'base.html.twig' %}

{% block title %}
    Nouvelle génération
    {{generation.model.manufacturer.name}}
    {{generation.model.name}}
{% endblock %}

{% block body %}

    <div class="container">
        <a href="{{ path('engine_show', {'slug' : engine.slug})}}">
            <i class="fi-xwllxl-arrow-simple-wide"></i>
            Retour
        </a>


        <div class="row py-5">
            <div class="col">
                <h1>Créer une nouvelle génération</h1>
                <h2 class="font-weight-light">{{generation.model.manufacturer.name}}
                    {{generation.model.name}}</h2>
                <br>
                {{ include('engine/_form.html.twig') }}
                <br>
                {{ include('engine/_delete_form.html.twig') }}
            </div>
        </div>

    </div>
{% endblock %}
