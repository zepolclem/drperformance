{% extends 'base.html.twig' %}

{% block title %}
    Nouvelle motorisation
    {{generation.model.manufacturer.name}}
    {{generation.model.name}}
{% endblock %}

{% block body %}

    <div class="container">
        <a href="{{ path('manufacturer_show', {'slug' : generation.model.manufacturer.slug})}}">
            <i class="fi-xwllxl-arrow-simple-wide"></i>
            Retour
        </a>


        <div class="row py-5">
            <div class="col">
                <h1>Créer une nouvelle motorisation</h1>
                <h2 class="font-weight-light">{{generation.model.manufacturer.name}}
                    {{generation.model.name}}</h2>
                <br>
                {{ include('engine/_form.html.twig') }}
            </div>
        </div>

    </div>
{% endblock %}
